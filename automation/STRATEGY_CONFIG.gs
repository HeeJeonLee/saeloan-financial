/**
 * ★★★ 새론금융 AI 자동화 시스템 - 전략 설정 파일 ★★★
 * 
 * 이희전 팀장님이 유일하게 수정하는 파일입니다.
 * 이 파일 하나만 바꾸면 모든 채널의 전략이 동시에 바뀝니다.
 * 
 * 수정 방법: 아래 값만 바꾸고 저장하세요.
 * AI가 나머지 모든 것을 자동으로 처리합니다.
 */

var STRATEGY = {

  // ══════════════════════════════════════════
  // 1. 현재 주력 상품 (하나만 선택: true)
  // ══════════════════════════════════════════
  FOCUS: {
    자영업자추가자금: true,    // 자영업자 생활안정자금 한도 초과분
    구입자금브릿지: true,     // 아파트 구입 잔금 브릿지
    신협금고전환: false,      // 대부업 → 신협·금고 전환
    경매낙찰잔금: false,      // 경매 낙찰 후 잔금 조달
  },

  // ══════════════════════════════════════════
  // 2. 타겟 지역 (현재 공략 지역)
  // ══════════════════════════════════════════
  TARGET_AREA: "서울·수도권 아파트",   // 예: "서울 강남·서초·송파" 또는 "경기 전지역"

  // ══════════════════════════════════════════
  // 3. 콘텐츠 톤 (AI가 글쓰는 스타일)
  // ══════════════════════════════════════════
  TONE: "신뢰감 있고 전문적으로",  
  // 다른 옵션: "친근하고 쉽게" / "숫자와 데이터 중심으로" / "고급스럽고 권위있게"

  // ══════════════════════════════════════════
  // 4. 게시 빈도
  // ══════════════════════════════════════════
  POST_FREQUENCY: {
    텔레그램: "매일 오전 9시",
    네이버블로그: "주 3회 (월·수·금)",
    카카오채널: "주 2회 (화·목)",
  },

  // ══════════════════════════════════════════
  // 5. 현재 특별 프로모션 (없으면 null)
  // ══════════════════════════════════════════
  PROMOTION: null,
  // 예시: "이번 달 신규 상담 고객 중도상환수수료 없는 상품 우선 안내"

  // ══════════════════════════════════════════
  // 6. 연락처 정보 (변경 시 여기서만 수정)
  // ══════════════════════════════════════════
  CONTACT: {
    전화: "010-5927-9205",
    대표전화: "1555-2137",
    홈페이지: "saeloan.co.kr",
    등록번호: "2026-수원-2324",
  },

  // ══════════════════════════════════════════
  // 7. AI 엔진 설정 (처음 1회 설정 후 건드리지 마세요)
  // ══════════════════════════════════════════
  AI: {
    OPENAI_API_KEY: "YOUR_OPENAI_API_KEY",   // OpenAI 가입 후 발급 (월 $1~3)
    MODEL: "gpt-4o-mini",                     // 가장 저렴하고 빠른 모델
    CONTENT_LANGUAGE: "한국어",
  }
};
